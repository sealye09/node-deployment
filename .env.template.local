# 远程服务器 SSH 连接信息（格式：user@host 或 user@ip）
SERVER=username@123.123.123.123

# 远程部署目录（建议为绝对路径，且该用户有写权限）
REMOTE_DIR=/home/youruser/your/project/path

# 本地构建命令（如 npm run build、pnpm run build:prod 等）
BUILD_COMMAND="pnpm run build:prod"

# 构建产物目录（相对于项目根目录，通常为 dist 或 build）
DIST_PATH=dist

# 远程备份保留数量（1-100 之间的整数，超出数量会自动删除最旧备份）
BACKUP_COUNT=5

# ================= 使用说明 =================
# 1. 部署前请复制本模板为 .env.test.local 和 .env.production.local 或者 .env.local
# 并根据实际环境分别填写变量。
# 2. 确保服务器已创建 REMOTE_DIR 目录，并赋予对应用户写权限。
# 3. 推荐使用权限受限的 Linux 用户进行部署，避免使用 root。
# 4. 确保本地已安装 ssh、scp、unzip 等命令行工具。
# 5. BUILD_COMMAND 必须能在本地构建出 DIST_PATH 目录。
# 6. 如需自定义构建产物目录，修改 DIST_PATH。
# 7. 备份文件将存放于 $REMOTE_DIR/__backups__ 目录下。
# 8. 所有变量均为必填项，否则部署会报错
